# Assets

Store reviewed local website assets under these directories when needed:

- `fonts/` for self-hosted fonts and their license notices;
- `images/` for production images; and
- `vendor/` for pinned third-party browser dependencies and licenses.

The initial migration keeps existing CDN dependencies unchanged. See
[external dependencies](../docs/external-dependencies.md) before adding or
updating vendored files.
